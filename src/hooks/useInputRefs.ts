import React, { useRef } from "react"

export const useInputRefs = (inputsCount: number) => {
  const input1ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input2ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input3ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input4ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input5ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input6ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input7ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input8ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input9ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input10ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input11ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input12ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input13ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input14ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input15ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input16ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input17ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input18ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input19ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input20ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input21ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input22ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input23ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input24ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input25ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input26ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input27ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input28ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input29ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input30ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input31ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input32ref = useRef() as React.MutableRefObject<HTMLInputElement>
  const input33ref = useRef() as React.MutableRefObject<HTMLInputElement>

  const inputRefs = [
    input1ref,
    input2ref,
    input3ref,
    input4ref,
    input5ref,
    input6ref,
    input7ref,
    input8ref,
    input9ref,
    input10ref,
    input11ref,
    input12ref,
    input13ref,
    input14ref,
    input15ref,
    input16ref,
    input17ref,
    input18ref,
    input19ref,
    input20ref,
    input21ref,
    input22ref,
    input23ref,
    input24ref,
    input25ref,
    input26ref,
    input27ref,
    input28ref,
    input29ref,
    input30ref,
    input31ref,
    input32ref,
    input33ref,
  ]

  inputRefs.length = inputsCount

  return inputRefs
}
